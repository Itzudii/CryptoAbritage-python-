# Core dependencies
requests==2.31.0
python-dotenv==1.0.1

# API server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Optional: WebSocket support (FastAPI includes websockets via starlette)
websockets==12.0

# Optional: Notifications
python-telegram-bot==20.7

# Dev tools (optional)
pytest==7.4.3
pytest-cov==4.1.0
black==23.12.1
flake8==7.0.0
mypy==1.8.0
