# Copy this file to `.env` and fill in the values

# --- Exchange Credentials (REQUIRED for live trading) ---
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Dashboard / API Security ---
# Random string used to protect admin endpoints in `web/api.py`
DASHBOARD_API_KEY=

# --- CORS for dashboard (comma-separated list or * for all) ---
API_ALLOWED_ORIGINS=http://localhost:8000, http://127.0.0.1:8000

# --- Notifications (optional) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Notes:
# - Keep this file out of version control. Use `.env` locally and in deployment secrets.
# - When `USE_TESTNET` is True in `config.py`, API keys are not strictly required.
